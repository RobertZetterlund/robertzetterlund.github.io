import React from "react";
import styled from "styled-components";

export const Info = () => {
  return <Main>yo</Main>;
};

const Main = styled.div`
  display: grid;
  place-items: center;
  position: relative;
  padding-bottom: 50px;
`;
